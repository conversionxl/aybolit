import { css, html, LitElement } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('cxl-tooltip-button')
export class CXLTooltipButtonElement extends LitElement {
  static get styles() {
    return [css``];
  }

  render() {
    return html``;
  }
}
